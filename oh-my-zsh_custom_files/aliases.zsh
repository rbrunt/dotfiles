# Set up some useful aliases to make long, commonly used commands easier to type

# SSH into vega
alias vegassh="ssh -X mppz24@vega.dur.ac.uk"
alias veggasshnox="ssh mppz24@vega.dur.ac.uk"

# NodeJS http-server (like python's simple http server, only not horrendously slow)

alias nodehttp="http-server"
